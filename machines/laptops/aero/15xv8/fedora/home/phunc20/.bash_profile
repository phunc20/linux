# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs
EDITOR=vimx
PATH="$PATH:/home/phunc20/.useful-scripts"
LS_COLORS="$LS_COLORS:di=1;33"
