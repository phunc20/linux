It seems that, to backup a firefox (it's profile, passwords, etc.),
it suffices to copy the directory `$HOME/.mozilla/`.

People recommend
1. Compress it (say, `tar` and then either `bzip` or `gzip`)
1. Encrypt it (say, using GPG)
